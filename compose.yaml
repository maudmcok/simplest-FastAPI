services:
  api:
    image: python:3.9
    container_name: fastapi-application
    command: > /bin/sh -c "uvicorn app.main:app --host 0.0.0.0 --port 7587"
    volumes:
      - ./app:/app
    ports:
      - '7587:7587'
    restart: "always"

    
